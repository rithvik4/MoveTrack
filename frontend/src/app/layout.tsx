import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'MoveTrack - Modern Fitness Platform',
  description: 'Track your walks and runs, join virtual events, and connect with friends',
  keywords: 'fitness, running, walking, gps, tracking, health, exercise',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}